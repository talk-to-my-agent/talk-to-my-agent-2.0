{
  "name": "talk-to-my-agent",
  "version": "1.0.0",
  "description": "A Chrome extension that generates cover letters using Gemini API",
  "main": "background.js",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "type": "module",
  "jest": {
    "transform": {},
    "testMatch": [
      "**/?(*.)+(spec|test).[jt]s?(x)",
      "**/?(*.)+(spec|test).mjs"
    ]
  }
}
